package dragger;

public class Operator {
	/*
	 * public static void main(String[] args) {
	 * 
	 * QueryColumn filmTitleColumn = new QueryColumn(1, "שם סרט", "title", null,
	 * null); QueryColumn filmKindColumn = new QueryColumn(2, "סוג סרט", "kind",
	 * null, null); QueryColumn actorNameColumn = new QueryColumn(3, "שם שחקן",
	 * "name", null, null);
	 * 
	 * QueryColumn actorIdColumn = new QueryColumn(4, "מזהה שחקן", "id", null,
	 * null); QueryColumn filmCodeColumn = new QueryColumn(5, "קוד סרט", "code",
	 * null, null); QueryColumn filmColumn = new QueryColumn(6, "קוד סרט",
	 * "film", null, null); QueryColumn actorColumn = new QueryColumn(7,
	 * "מזהה שחקן", "actor", null, null);
	 * 
	 * QuerySource filmsSource = new QuerySource(1, "סרטים",
	 * Arrays.asList(filmCodeColumn, filmKindColumn, filmTitleColumn), "films",
	 * null, null); QuerySource actorsSource = new QuerySource(3, "שחקנים",
	 * Arrays.asList(actorIdColumn, actorNameColumn), "actors", null, null);
	 * QuerySource actorsInMoviesSource = new QuerySource(2, "שחקנים בסרטים",
	 * Arrays.asList(filmColumn, actorColumn), "actors_in_films", null, null);
	 * 
	 * SourceConnection filmsToActorsInFilms = new SourceConnection(1,
	 * filmCodeColumn, filmColumn, filmsSource, actorsInMoviesSource);
	 * SourceConnection actorsToActorsInFilms = new SourceConnection(2,
	 * actorIdColumn, actorColumn, actorsSource, actorsInMoviesSource);
	 * 
	 * QueryGenerator generator = new RationalQueryGenerator(); Query query =
	 * new Query(Arrays.asList(actorNameColumn, filmTitleColumn),
	 * Arrays.asList(filmsSource, actorsSource, actorsInMoviesSource),
	 * Arrays.asList(filmsToActorsInFilms, actorsToActorsInFilms));
	 * 
	 * System.out.println(generator.generate(query)); }
	 */
}